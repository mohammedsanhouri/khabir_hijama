<!--<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Security Group -->
    <record id="group_branch_user" model="res.groups">
        <field name="name">Branch User</field>
    </record>

    <!-- Record Rule for hijama -->
    <record id="branch_hijama_rule" model="ir.rule">
        <field name="name">Branch-specific Hijama Access</field>
        <field name="model_id" ref="model_khabir_hijama"/>
        <field name="domain_force">
            [('journal_id.branch_id', '=', user.branch_id.id)]
        </field>
        <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Attach menu to branch users only -->
    <menuitem id="khabir_hijama_main" name="Hijama"
        groups="khabir_hijama.group_branch_user"/>
    
</odoo>
-->